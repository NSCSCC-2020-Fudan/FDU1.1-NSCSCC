name: svlint

on: [push, pull_request]

jobs: 
  build:

    runs-on: ubuntu-latest

    steps:
    - name: SystemVerilog Linter
      uses: dalance/svlint-action@v1.0.2
    - run: svlint ./* --config .svlint.toml